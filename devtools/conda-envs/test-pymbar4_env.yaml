name: test-pymbar4
channels:
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip

    # Testing and development
  - pytest
  - pytest-cov
  - pytest-regressions
  - gromacs ~=2023.4

    # Standard dependencies
  - numpy
  - scipy
  - matplotlib
  - pymbar =4
